const TABLE_HEADERS = [
  "Agent Name",
  "Agent Email",
  "Status",
  "Last Seen",
  "Actions",
];

// source: https://stackoverflow.com/a/50343015
const EMAIL_REGEX = /^[^@]+@[^@]+\.[^@]+$/;

export { TABLE_HEADERS, EMAIL_REGEX };
